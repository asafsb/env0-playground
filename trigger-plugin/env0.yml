version: 2
deploy:
  onSuccess:
    - name: Trigger Envrionment
      use: https://github.com/env0/env0-project-level-workflow-triggers
      inputs:
        downstream-environment: ${TRIGGER_ENVIRONMENT}

destroy:
  onSuccess:
    - name: Trigger Envrionment
      use: https://github.com/env0/env0-project-level-workflow-triggers
      inputs:
        downstream-environment: ${TRIGGER_ENVIRONMENT}
