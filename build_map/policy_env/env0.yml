version: 2
shell: bash

deploy:
  steps:
   setupVariables:
     after:
       - name: "Create"
         run: |
           touch test.txt
           for var in "${!policy@}"; do echo $var  ; done
           cat test.txt
