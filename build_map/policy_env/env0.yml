version: 2
shell: 
  type: bash

deploy:
  steps:
   setupVariables:
     after:
       - name: "Create"
         run: |
           for var in "${!policy_@}"; do
             printf '%s=%s\n' "$var" "${!var}"
           done